import { SmallpricePipe } from './smallprice.pipe';

describe('SmallpricePipe', () => {
  it('create an instance', () => {
    const pipe = new SmallpricePipe();
    expect(pipe).toBeTruthy();
  });
});
